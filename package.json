{
    "dependencies": {
        "axios": "^1.6.8",
        "json-server": "^0.17.4"
    },
    "scripts": {
        "start:db": "json-server -w db.json",
        "db": "npm run start:db",
        "start:parcel": "npx parcel index.html",
        "parcel": "npm run start:parcel"
    },
    "devDependencies": {
        "buffer": "^6.0.3"
    }
}
